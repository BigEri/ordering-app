# Kiosk release APK

Umístěte sem signed release jako `tableflow-kiosk.apk`.

URL: `{NEXT_PUBLIC_APP_URL}/releases/tableflow-kiosk.apk`

Aktuální release: **1.4** (`versionCode` 5), **debug-signed** (stejný podpis jako instalace ze Studia). Na Vercelu: `KIOSK_APK_VERSION_CODE=5`, `KIOSK_APK_SHA256` z `node scripts/publish-kiosk-apk-hashes.mjs`.
