# Kiosk release APK

Umístěte sem signed release jako `tableflow-kiosk.apk`.

URL: `{NEXT_PUBLIC_APP_URL}/releases/tableflow-kiosk.apk`

Aktuální release: **1.4** (`versionCode` 5). Výchozí `base_url` v APK: `https://app.tableflow.cz` (staré `*.vercel.app` se při startu přepíší). Na Vercelu: `KIOSK_APK_VERSION_CODE=5`, `KIOSK_APK_VERSION_NAME=1.4`, volitelně SHA256 z `node scripts/publish-kiosk-apk-hashes.mjs`.
